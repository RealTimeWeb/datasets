Music
    JSON - list of complicated dictionaries
Geocode
    API - returns highly structured data that is parsed down to two values
Crimes
    JSON - list of departments/states, with sublist of years of of structured data
Reddit
    API - returns highly structured data
Yelp
    JSON - list of businesses with a sublist of reviews
Cancer
    DB - three tables of data (age, area, site) that let you get data broken down by the categories (so list of dictionaries)
Scripts
    JSON - indexed by the movie title (which is currently a weird index), you can get back a complicated dictionary, including a list of lines. Sampling is done at the top level only.
Airports
    JSON - list of dictionaries. the dictionaries are nested dictionaries, so a little complicated.
Fuel
    